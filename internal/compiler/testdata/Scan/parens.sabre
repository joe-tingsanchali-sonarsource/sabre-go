// Parentheses and brackets
array := [10]int32{1, 2, 3}
matrix := mat4x4{{1, 0, 0, 0}, {0, 1, 0, 0}}
function_call := sqrt(x * x + y * y)
grouping := (a + b) * (c - d)

// Braces
block := {
    nested := {
        deeply := {
            value
        }
    }
}
